language: minimal

addons:
  apt:
    packages:
    - jq

script:
  - curl -fsSLo release.sh https://raw.githubusercontent.com/BigWigsMods/packager/master/release.sh
  - chmod +x release.sh
  - ./release.sh
  - ./release.sh -g 1.13.2
